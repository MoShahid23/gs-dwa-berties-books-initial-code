<!doctype html>
<html>
  <head>
    <title>Books we sell - <%=shopName%></title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>
  <body>
    <h1>Welcome to <%=shopName%></h1>
    <h2>Here are the books we sell:</h2>
    <ul>
      <% for(let i=0; i < availableBooks.length; i++) { %>
          <li><span><%=availableBooks[i].name%></span>, £<%=availableBooks[i].price%></li>
      <% } %>
    </ul>
  </body>
</html>
